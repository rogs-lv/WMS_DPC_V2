import { FilterModulePipe } from './filter-module.pipe';

describe('FilterModulePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModulePipe();
    expect(pipe).toBeTruthy();
  });
});
