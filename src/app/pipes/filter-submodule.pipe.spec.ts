import { FilterSubmodulePipe } from './filter-submodule.pipe';

describe('FilterSubmodulePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubmodulePipe();
    expect(pipe).toBeTruthy();
  });
});
