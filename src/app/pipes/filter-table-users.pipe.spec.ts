import { FilterTableUsersPipe } from './filter-table-users.pipe';

describe('FilterTableUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTableUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
